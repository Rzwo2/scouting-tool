{% extends 'main.html.twig' %}

{% block title %}Profil{% endblock %}

{%- block headline -%}Profil{%- endblock -%}

{% block main %}
<article class="profile-section">
    <h2>Kontoinformationen</h2>
    <div class="overview-grid">
        <span class="property">Benutzername</span>
        <span class="value">{{ app.user.username }}</span>
        <span class="property">Email-Adresse</span>
        <span class="value">{{ app.user.email }}</span>
    </div>
    <div class="profile-actions">
        <button type="button" commandfor="dialog-username" command="show-modal">Benutzername ändern</button>
        <button type="button" commandfor="dialog-password" command="show-modal">Passwort ändern</button>
    </div>
</article>

<dialog id="dialog-username" closedBy="any">
    <header>
        <h2>Benutzername ändern</h2>
        <button type="button" class="dialog-close" aria-label="Schließen" commandfor="dialog-username" command="close">&times;</button>
    </header>
    {{ form_start(usernameForm, {action: path('app_profile_change_username'), method: 'POST'}) }}
    {{ form_widget(usernameForm) }}
    {{ form_end(usernameForm) }}
</dialog>

<dialog id="dialog-password" closedBy="any">
    <header>
        <h2>Passwort ändern</h2>
        <button type="button" class="dialog-close" aria-label="Schließen" commandfor="dialog-password" command="close">&times;</button>
    </header>
    {{ form_start(passwordForm, {action: path('app_profile_change_password'), method: 'POST'}) }}
    {{ form_widget(passwordForm) }}
    {{ form_end(passwordForm) }}
</dialog>

{% endblock main %}

